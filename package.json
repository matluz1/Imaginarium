{
  "name": "imaginarium",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "deploy": "docker-compose up --build"
  },
  "author": "matluz",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
