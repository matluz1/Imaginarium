{
  "name": "imaginarium",
  "version": "1.0.0",
  "scripts": {
    "dev": "docker-compose -f docker-compose.dev.yml up --build",
    "deploy": "docker-compose up --build"
  },
  "author": "matluz",
  "devDependencies": {
    "prettier": "3.2.2"
  }
}
